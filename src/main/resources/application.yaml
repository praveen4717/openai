spring:
  application:
    name: openai
  ai:
    openai:
      api-key: ${OPEN_API_KEY}
logging:
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG